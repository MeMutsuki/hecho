# 実装計画

## 現在地

- [x] 初期構成（Vite + React + TS + Dexie）
- [x] GitHub リポジトリ + CI（push で lint と build が走る）
- [x] Vercel デプロイ（push するだけで本番が更新される）
- [x] iPhone のホーム画面にインストール、オフライン起動、データ永続化を実機確認
- [x] セッション 1〜5（習慣登録・記録・タイムライン・遡り入力・頻度表示）
- [x] セッション 6〜8（レビューで見つかった 3 点）
- [ ] セッション 9（仕上げ）

**配布経路はもう通っている。以降リリース手段のことは考えなくてよい。**

## 進め方

1 機能 = 1 セッション。終わったらコミットして `/clear` する。
長く続けると文脈が濁って、関係ない場所を触り始めるため。

各セッションの終わりに必ず：

```bash
npm run lint && npm run build   # CI と同じものをローカルで
git add -A && git commit
git push                        # push した時点で本番に反映される
```

## セッション一覧

プロンプトはそのまま Claude Code に貼れる形にしてある。
**毎回「〜はまだ作らない」を明示すること。** CLAUDE.md に書いてあっても、
明示した方が確実に効く。

### 1. 習慣を登録する

> CLAUDE.md と FUTURE.md を読んで。v1スコープの1番目「習慣を登録する」だけを実装したい。
> 習慣の追加フォーム（名前・絵文字・色）と一覧表示を src/components/ に作って App.tsx から使って。
> タイムラインと記録機能はまだ作らない。src/db/db.ts の toggleLog も今は触らないで。

### 2. 今日の分をワンタップで記録

> v1スコープの2番目。今日の分をワンタップで記録できるようにしたい。
> 習慣一覧の各行をタップしたら toggleLog が走って、押された状態が見た目でわかるようにして。
> 確認ダイアログは挟まない。過去日への遡り入力とタイムラインはまだ作らない。

**ここまでできたら自分で使い始める。** 表示が未完成でも記録は貯まる。
実データが入った状態で 3 以降を作った方が、確実に良いものになる。

### 3. 横スクロールのタイムライン

> v1スコープの4番目。行=習慣・列=日付の横スクロールタイムラインを作りたい。
> CSS Grid と position: sticky で、左端の習慣ラベル列を固定して。
> 直近30日分を表示し、今日を右端にして初期スクロール位置も右端に。
> やっていない日は空欄のまま（× や薄いプレースホルダを置かない）。

### 4. 過去日への遡り入力

> v1スコープの3番目。タイムライン上のセルをタップして、過去日にも記録をつけられるようにして。
> 未来日のセルはタップできないようにする（date に未来日を入れない、という制約を守る）。

### 5. 頻度表示

> v1スコープの5番目。習慣ごとに「直近30日で N 回」を表示して。
> 左端の習慣ラベル列に出すのが自然だと思うけど、レイアウトが窮屈なら相談して。
> ストリーク（連続日数）は出さない。

### 6. 深夜0時をまたぐと today が古いままになるバグ

`todayKey()` はレンダー時に一度計算されるだけで、日付が変わっても更新されない。
PWA はホーム画面から起動すると常駐するので、23:50 に開いたまま 0:05 に記録すると
**昨日の日付に入る。** 寝る前の記録がこのアプリの主用途なので、いちばん踏みやすい。

> 深夜0時をまたぐと todayKey() が古いままになるバグを直したい。
> visibilitychange と、日付が変わるタイミングでの再計算を入れて、
> Timeline と HabitList の両方が新しい日付を見るようにして。他の機能は触らないで。

### 7. タイムラインのラベル列が広すぎる

iPhone 幅（375px）で実測したところ、スクローラ 333px のうちラベル列が 168px。
**一度に見えるのが 5 日分しかない。** 30 日の頻度を読むのに 6 画面スクロールが要る。
「頻度が目で読み取れる」がこのアプリの存在理由なので、ここが効いていないのは致命的。

実際に詰めた版を当てて 9 日分見えるようにしたら、
「風呂はほぼ毎日、ゴミ出しは空いている」がパッと読めるようになった。

タップ領域が小さくなる懸念は、今日の記録は習慣一覧の大きい行で行う設計なので問題ない。
タイムラインのセルは遡り修正用。

> タイムラインのラベル列が広すぎて、iPhone幅で5日分しか見えない。
> ラベル列を116px、セルを24px、お団子を14pxにして、
> 「直近30日で N 回」は数字だけを名前の右に置いて。
> 「直近30日」の文言はタイムラインの見出し側に1回出す。
> 左端の色ドットは絵文字と重複しているので削除。
> CSSだけで、Timeline.tsx のロジックは変えないで。

### 8. 習慣を編集・アーカイブする手段がない

`archivedAt` はスキーマにあって一覧のフィルタも書かれているのに、
**それを設定する導線がどこにもない。** 名前を打ち間違えたら永久に残る。
自分で使い続ける上でここは効く。

> 習慣をアーカイブする手段がない。archivedAt を設定する導線を習慣一覧に足して。
> 名前と絵文字の編集もできるようにしたい。削除ではないので過去のログは残す。
> FUTURE.md に「アーカイブした習慣の表示切り替え」があるけど、そっちはまだやらない。

### 9. 仕上げ

- README にスクリーンショット 2 枚 —— **1週間使った自分の実データで撮る。**
  ダミーデータより、READMEとしても実績としても強い
- デモ GIF 30 秒
- 使ってみて気づいた不便を直す（**これが一番大事**）
- FUTURE.md を埋める。作っている間に浮かんだ「あとで足したいもの」を書き出しておくと、
  ここから先の「v1 に入れるか」の判断が速くなる

## 日程

配布経路の疎通が予定より 4 日早く終わったので、その分の余裕がある。

| 期限                     | 内容                                                             |
| ------------------------ | ---------------------------------------------------------------- |
| 9/12                     | セッション 6〜8（バグ修正・レイアウト・アーカイブ）              |
| 9/19                     | セッション 9（仕上げ）                                           |
| 9/26                     | テンプレ抽出（このリポジトリから汎用部分を剥がしてスターター化） |
| 9/27                     | JPHACKS 本エントリー                                             |
| 10/10                    | Learning Day                                                     |
| 10/17-18 または 10/24-25 | Hack Day                                                         |

## 判断に迷ったら

- 機能を足したくなった → 手を動かす前に FUTURE.md に書く
- スコープを広げるか迷った → 広げない。v1 を終わらせることが目的
- 実装が重いと感じた → たぶんスコープが大きい。削れないか考える
