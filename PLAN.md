# 実装計画

## 現在地

- [x] 初期構成（Vite + React + TS + Dexie）
- [x] GitHub リポジトリ + CI（push で lint と build が走る）
- [x] Vercel デプロイ（push するだけで本番が更新される）
- [x] iPhone のホーム画面にインストール、オフライン起動、データ永続化を実機確認
- [ ] ここから機能を作る

**配布経路はもう通っている。以降リリース手段のことは考えなくてよい。**

## 進め方

1 機能 = 1 セッション。終わったらコミットして `/clear` する。
長く続けると文脈が濁って、関係ない場所を触り始めるため。

各セッションの終わりに必ず：

```bash
npm run lint && npm run build   # CI と同じものをローカルで
git add -A && git commit
git push                        # push した時点で本番に反映される
```

## セッション一覧

プロンプトはそのまま Claude Code に貼れる形にしてある。
**毎回「〜はまだ作らない」を明示すること。** CLAUDE.md に書いてあっても、
明示した方が確実に効く。

### 1. 習慣を登録する

> CLAUDE.md と FUTURE.md を読んで。v1スコープの1番目「習慣を登録する」だけを実装したい。
> 習慣の追加フォーム（名前・絵文字・色）と一覧表示を src/components/ に作って App.tsx から使って。
> タイムラインと記録機能はまだ作らない。src/db/db.ts の toggleLog も今は触らないで。

### 2. 今日の分をワンタップで記録

> v1スコープの2番目。今日の分をワンタップで記録できるようにしたい。
> 習慣一覧の各行をタップしたら toggleLog が走って、押された状態が見た目でわかるようにして。
> 確認ダイアログは挟まない。過去日への遡り入力とタイムラインはまだ作らない。

**ここまでできたら自分で使い始める。** 表示が未完成でも記録は貯まる。
実データが入った状態で 3 以降を作った方が、確実に良いものになる。

### 3. 横スクロールのタイムライン

> v1スコープの4番目。行=習慣・列=日付の横スクロールタイムラインを作りたい。
> CSS Grid と position: sticky で、左端の習慣ラベル列を固定して。
> 直近30日分を表示し、今日を右端にして初期スクロール位置も右端に。
> やっていない日は空欄のまま（× や薄いプレースホルダを置かない）。

### 4. 過去日への遡り入力

> v1スコープの3番目。タイムライン上のセルをタップして、過去日にも記録をつけられるようにして。
> 未来日のセルはタップできないようにする（date に未来日を入れない、という制約を守る）。

### 5. 頻度表示

> v1スコープの5番目。習慣ごとに「直近30日で N 回」を表示して。
> 左端の習慣ラベル列に出すのが自然だと思うけど、レイアウトが窮屈なら相談して。
> ストリーク（連続日数）は出さない。

### 6. 仕上げ

- README にスクリーンショット 2 枚
- デモ GIF 30 秒
- 使ってみて気づいた不便を直す（**これが一番大事**）

## 日程

配布経路の疎通が予定より 4 日早く終わったので、その分の余裕がある。

| 期限 | 内容 |
|---|---|
| 9/19 | セッション 1〜5 を終える |
| 9/23 | 仕上げ（README・スクショ・デモ GIF） |
| 9/26 | テンプレ抽出（このリポジトリから汎用部分を剥がしてスターター化） |
| 9/27 | JPHACKS 本エントリー |
| 10/10 | Learning Day |
| 10/17-18 または 10/24-25 | Hack Day |

## 判断に迷ったら

- 機能を足したくなった → 手を動かす前に FUTURE.md に書く
- スコープを広げるか迷った → 広げない。v1 を終わらせることが目的
- 実装が重いと感じた → たぶんスコープが大きい。削れないか考える
