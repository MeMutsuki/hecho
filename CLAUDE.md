# hecho!

## このプロダクトは何か

**やったこと（DONE）だけを記録するアプリ。** 未来の TODO は扱わない。

一般的なタスク管理アプリが「これからやること」を積む結果、未消化のタスクが山積みになって
気が滅入るのに対し、hecho! は過去から現在の「やった」だけを積み上げる。
繰り返し発生する物事（風呂・洗濯・ゴミ出し・課題・自主学習など）を、
頻度が目で読み取れる形で可視化することが目的。

**この反転が設計上の制約になっている。未来のレコードが存在しないので、
「積み残し」という状態がデータモデル上つくれない。** ここを崩す変更は入れないこと。

## v1 のスコープ（これ以外を実装しない）

1. 習慣を登録する（名前・絵文字・色）
2. 今日の分をワンタップで記録する（確認ダイアログを挟まない）
3. 過去日にも遡って記録できる
4. 横スクロールのタイムライン表示（行 = 習慣 / 列 = 日付 / 左列は sticky で固定）
5. 習慣ごとの「直近 30 日で N 回」表示

## やらないこと（提案もしない）

- **サーバー・バックエンドを作らない。** v1 はブラウザ内 IndexedDB のみで完結する。
- **認証・ログインを入れない。** 単一利用者・単一端末を前提にする。
- **通知・リマインダーを入れない。** 催促するアプリではない。
- **ストリーク（連続日数）を主役にしない。** 1 日途切れた瞬間に数字がゼロに戻る表現は、
  このアプリが避けたい体験そのもの。主役は累積回数と直近頻度。表示するとしても副次的に。
- **「やっていない」を強調表示しない。** 空セルは空のまま。× や薄いプレースホルダを置かない。
- 上記以外にも、`FUTURE.md` に載っている項目は v1 では実装しない。
  実装したくなったら、まず FUTURE.md から外す提案をして合意を取ること。

## データモデル

テーブルは 2 つだけ（`src/types.ts` と `src/db/db.ts`）。

- `habits` — 習慣の定義。タイムラインの 1 行に対応。
- `logs` — 「やった」記録。主キーは `${habitId}:${date}` で、
  同じ日の二重記録を構造的に防いでいる。**`date` に未来日を入れない。**

日付キーは常にローカルタイムゾーンの `'YYYY-MM-DD'`。
`toISOString()` は UTC に変換されて日付がずれるので使わない。
日付操作は `src/lib/date.ts` のヘルパーを経由すること。

## 技術構成

- Vite + React 19 + TypeScript
- Dexie (IndexedDB) — `dexie-react-hooks` の `useLiveQuery` で購読する
- PWA は手書きの `public/manifest.webmanifest` + `public/sw.js`（依存ゼロ）
- デプロイ先は Vercel

## コマンド

```bash
npm run dev      # 開発サーバー
npm run build    # 型チェック + 本番ビルド
npm run preview  # 本番ビルドをローカル確認（Service Worker の確認はこちら）
npm run lint
npm run format
```

## 規約

- スタイルは `src/index.css` の CSS 変数ベース。CSS-in-JS やフレームワークは入れない。
- コメントは日本語で、「なぜそうしたか」を書く。「何をしているか」はコードを読めばわかる。
- 1 機能ごとにコミットする。複数機能をまたぐ変更を 1 コミットに混ぜない。
- `src/App.tsx` の現在の中身は疎通確認用の仮画面。タイムライン実装時に差し替える。
